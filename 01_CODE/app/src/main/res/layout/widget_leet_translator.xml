<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/widget_background"
    android:gravity="center">

    <!-- Widget Title -->
    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Simple Leet"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@color/widget_text_primary"
        android:gravity="center"
        android:layout_marginBottom="8dp" />

    <!-- App Icon -->
    <ImageView
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/ic_custom_mode"
        android:layout_marginBottom="8dp"
        android:contentDescription="Leetspeak Generator" />

    <!-- Example Translation -->
    <TextView
        android:id="@+id/widget_example"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Hello → #3110"
        android:textSize="14sp"
        android:textColor="@color/widget_text_secondary"
        android:gravity="center"
        android:layout_marginBottom="8dp" />

    <!-- Tap to open hint -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tippen zum Öffnen"
        android:textSize="12sp"
        android:textColor="@color/widget_text_hint"
        android:gravity="center"
        android:alpha="0.7" />

</LinearLayout>